# Verification

Checks marked **29 August 2026** cover the single player scheme: WASD movement, mouse aiming and held left mouse button firing. Native input checks used a separate Edge tab at 1720×966, which was closed afterwards. Other entries retain the 28 August baseline, checked with Node.js 24.19.0 and the local Chromium browser preview.

| Ref | Check | Result |
| --- | --- | --- |
| V1 | `npm test` | **29 August:** all 67 tests pass, including 16 control tests and seven casing tests. Coverage includes explicit pointer aim, no automatic player firing or retargeting, held/released fire, heading fallback, Demo targeting and casing motion, visibility and lifecycle. Existing combat, presets, projection, animation and rendering checks also pass. |
| V2 | JavaScript syntax | **29 August:** `node --check` passes for all 10 JavaScript files under `src/` and `scripts/`. |
| V3 | Local HTTP serving | HTML, JavaScript and CSS return 200. Unknown files, Git metadata, server source and a traversal request return 404. |
| V4 | Bare, Juiced and Overdrive | Browser controls select 0, 16 and 27 effects respectively. Juiced leaves every combat/timing switch off. Demo runs score takedowns and update shot/time counters. |
| V5 | Pause and keyboard focus | P resumes from the Pause button; Escape pauses from the canvas. Run statistics stay fixed while paused. Game shortcuts are blocked inside Options so its controls cannot accidentally restart or resume a run. |
| V6 | Reduced camera motion | Overdrive leaves all five protected motion switches off and disabled, with the other 22 effects enabled. |
| V7 | Audio integration | Sound initialises after a control gesture; the UI reports it ready, paused and off as appropriate. Audible quality has not been assessed by a human listener. |
| V8 | Visual inspection | Checked the initial screen, Bare gameplay and the Overdrive aftermath. Pausing leaves the arena visible. No browser errors or warnings were captured during these checks. |
| V9 | Restart and defeat | **29 August, native Edge:** a stationary run ended at 0:06 with zero shots. Enter started a fresh run with five health, zero time and zero score. The 28 August check also confirmed that restart resets time, score and health. |
| V10 | Preview layout | Checked 1280×720, 390×600, 320×480 and 844×390 viewports without horizontal overflow. The panel body scrolls independently; its header and close button stay visible, including when tabbing to lower controls. Narrow viewport checks do not add touch movement controls. |
| V11 | Single player scheme | **29 August, native Edge:** 1.2 seconds idle produced zero shots. A left click fired once; the counter stayed at one after another 0.9 seconds. WASD taps moved the fox while its aim stayed on the stationary pointer. Space did not fire; repeated ArrowRight input did not move the fox. No control selector remains. |
| V12 | Stronger muzzle flash | Visually checked a single shot with the larger flame, bright core and local glow, then the same firing input with the effect off. The flash lasts 130 ms and remains separate from combat rules. No browser errors or warnings were captured. |
| V13 | Gun size and recoil | Visually checked the enlarged gun at rest, after a shot, with muzzle flash, and with recoil disabled. The tilted art retains a large weapon and the 17-unit recoil. Its resting muzzle position and physical player kickback are unchanged. |
| V14 | Tilted character art | Inspected all eight fox headings in a temporary contact sheet, including 0.44× laptop scale. Compared two animated tail poses and the full recoil pose. Rounded characters, raised heads, cream tail tips and grounded shadows remain distinct. The temporary sheet is not shipped with the game. |
| V15 | Tilted gameplay | A presentation-only Demo run scored four takedowns with 17 shots. Overdrive with reduced motion left all five protected switches off and disabled (22/27 effects). The game and art preview reported no browser errors or warnings. |
| V16 | Immersive layout | At 1280×720, the stage fills the window and the proportional canvas measures 1152×720. The options panel is 390×692 with independent content scrolling. Opening it leaves the canvas width unchanged. There is no page overflow. |
| V17 | Options lifecycle | **29 August, native Edge:** J opened Options and paused at 0:01 and one shot. Time and shots stayed fixed for 0.6 seconds; P and R inside the panel did nothing. Native Escape resumed the same run with canvas focus, the same stationary mouse aim and no extra shot. The 28 August checks also confirmed that ready and manually paused runs stay in their original states. |
| V18 | Separate Demo | **29 August, native Edge:** with Options open, Demo advanced from two to seven shots and 0:00 to 0:01. Switching Demo off left the counter at seven through another 0.6 seconds. No browser errors or warnings were captured during the native input checks. |
| V19 | Input lifecycle in an isolated VM | **29 August:** a separate event-handler smoke check passed WASD-only mappings, left-button tap/hold, mixed-button releases, cancel/blur, retained pointer aim across restart/resume, panel input guards and focus, no retained firing or click-through shots, and Demo isolation. This used a DOM stub, not native browser input. |
| V20 | Build and markup | **29 August:** `npm run build` succeeds. Every static `byId('...')` reference resolves in `index.html`; no control-mode element remains. The 1200×630 social image was visually checked with its updated WASD + mouse footer. |
| V21 | Shell casing flight | **29 August:** inspected actual renderer frames at 0.8× laptop scale, plus all eight launch headings with gun recoil on and off. Brass rises from weapon height, spins, casts a floor shadow, bounces once and settles. Sample trajectories at 20, 30, 60 and 120 frames per second reached the same peak and settled within 1.25–1.3 seconds. Native Edge Demo showed airborne and settled brass; toggling Shell casings off hid both brass and shadows while paused. No browser errors or warnings were captured. Unit tests cover rotated launch positions, height projection, pause, hidden motion, the 600-casing cap, reset and unchanged combat state. |

Native browser checks did not cover sustained mouse holds or right-clicks; the VM results are listed separately. This is an initial prototype check, not a cross-browser or accessibility certification. Safari, Firefox, touch gameplay and controller input have not been tested. Player controls require a keyboard and mouse.
